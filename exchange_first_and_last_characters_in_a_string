"""
This code returns and prints out a new string from a given string (given_string) where the first and last
characters have been exchanged.
"""

given_string = 'Python'


def mix_front_back(given_string):
    if len(given_string) <= 1:
        return given_string
    if len(given_string) > 0:
        return given_string[-1:] + given_string[1:-1] + given_string[:1]


print(mix_front_back(given_string))
