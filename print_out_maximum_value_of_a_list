"""
This code examines numbers from a given list (original_list) and finds the highest number (max_value)
and prints it out.
"""


def maximum_selection():
    indx = 0
    max_value = original_list[indx]
    for number in original_list:
        if number > max_value:
            indx += 1
            max_value = number
    return max_value


original_list = [2, 3, 4, 5, 1, 10, 6, 7, 8, 9]

max_value = maximum_selection()

print(max_value)
